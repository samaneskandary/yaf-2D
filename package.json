{
  "name": "soccer-stars",
  "version": "1.0.0",
  "private": true,
  "description": "ساکر استارز — فوتبال دو بُعدی آفلاین (PWA + Capacitor)",
  "scripts": {
    "android:add": "cap add android",
    "android:sync": "cap sync",
    "android:open": "cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
